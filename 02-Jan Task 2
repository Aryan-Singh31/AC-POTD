#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,k;
    cin>> n >> k;
    int c=0,num;
    int arr[n];
    for(int i=0;i<n;i++)
    {
        cin>>arr[i];
    }
   num=arr[k-1];
   for(int i=0;i<n;i++)
   {
    if(arr[i]>0&&arr[i]>=num)
    c++;
   }
   cout<< c;
   return 0;
}
